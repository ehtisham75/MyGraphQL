import { View, Text } from 'react-native'
import React from 'react'

const TestScreen = () => {
    return (
        <View>
            <Text>TestScreen</Text>

            {/* <TouchableOpacity
        onPress={() => { alert(data?.Query) }}
        style={{
          width: wp(70),
          height: hp(7),
          backgroundColor: Colors.PRIMARY_COLOR,
          alignItems: 'center',
          justifyContent: 'center',
          borderRadius: hp(1),
          marginTop: hp(10),
        }}>
        <Text style={{
          fontSize: hp(2.2),
          color: Colors.WHITE_TEXT_COLOR
        }}>Show Data</Text>
      </TouchableOpacity> */}
        </View>
    )
}

export default TestScreen